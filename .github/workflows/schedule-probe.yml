name: Schedule Probe

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - name: Print trigger
        run: |
          echo "event=${GITHUB_EVENT_NAME}"
          date -u
